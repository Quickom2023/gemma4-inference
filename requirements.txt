# NOTE: torch / torchvision / torchaudio are installed separately in install.sh
# with explicit +cu128 pins (RTX 5080 / Blackwell). Do not add them here — a PyPI
# torch (CUDA 13) mixed with a cu128 torchaudio causes a CUDA-mismatch RuntimeError.

# Gemma 4 multimodal stack
transformers>=4.57.0
accelerate
librosa            # audio track handling in videos
av                 # PyAV video decoder backend used by transformers
torchcodec==0.6.*  # decodes AUDIO from mp4 (load_audio_from_video); 0.6 matches torch 2.8/cu128
pillow
huggingface_hub
bitsandbytes>=0.47.0   # 4-bit/8-bit quantization (--4bit); needs recent build for Blackwell/sm_120

# HTTP API (api.py / service.py)
fastapi
uvicorn[standard]
python-multipart   # required by FastAPI for multipart/form-data uploads
